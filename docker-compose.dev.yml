services:
  app:
    build:
      dockerfile: Dockerfile.dev
    container_name: tb-script-frontend-dev
    env_file:
      - .env.dev
    ports:
      - "${VITE_EXPOSED_HOST}:${VITE_EXPOSED_PORT}:80"
    volumes:
      - ./src:/app/src
    stdin_open: true
    tty: true